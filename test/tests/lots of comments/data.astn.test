/*a newline block comment at the beginning*/
//a newline line comment at the beginning
! //a newline line comment after the exclamation mark
/*a newline block comment after the exclamation mark*/
/*a newline block comment before schema open paren, should be ignored*/ ( //a same line comment after schema open paren, should be ignored
    'component types': {
        'root': (
            /*a block comment in the schema, should be ignored*/
            'node': (
                'properties': {
                    'c': (
                        'type': | 'value' (
                            'quoted': false
                            'default value': ""
                        )
                    )
                }
            )
        )
    }
    'root type': 'root'
) //a same line line comment after the schema close paren


(
    /*a newline block comment before the first entry*/
    'c': C
    /*a newline block comment before an unknown prpperty, should be ignored*/
    'b': 42
) //a same line line comment after the instance data close paren

/*a newline block comment at the end*/