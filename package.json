{
  "name": "mrshl",
  "version": "0.0.1",
  "description": "marshalling/unmarshalling (~serialiazation/deserialization) of data to/from JSON or extended JSON",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir .\\dist & rd /s /q .\\dist && tsc",
    "lint": "tslint --project tsconfig.json",
    "buildLintAndTest": "npm run build && npm run lint && npm run test",
    "validatePublishReadiness": "git diff --exit-code && ncu -u --packageFile package.json && npm install && npm run buildLintAndTest",
    "pubMin": "npm run validatePublishReadiness && npm version minor && git push && npm publish",
    "pubPatch": "npm run validatePublishReadiness && npm version patch && git push && npm publish",
    "test": "tsc && node ./dist/test",
    "u2l": "ncu -u --packageFile package.json && nsi"
  },
  "keywords": [],
  "author": "****",
  "license": "ISC",
  "types": "dist/index.d.ts",
  "prepublish": "tsc",
  "repository": {
    "type": "git",
    "url": "https://github.com/corno/mrshl.git"
  },
  "files": [
    "dist/*"
  ],
  "dependencies": {
    "@types/mocha": "^7.0.1",
    "@types/node": "^13.7.0",
    "bass-clarinet": "^0.21.0"
  },
  "devDependencies": {
    "mocha": "^7.0.1"
  }
}
